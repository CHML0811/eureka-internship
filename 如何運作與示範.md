# 如何運作與示範 (How It Works & How to Demo)

> 給 Leo 的白話說明：這份文件講的不是「每一行程式怎麼寫」，而是
> 「為什麼這樣設計、資料怎麼流動、安全在哪裡把關」。
> 張博士 (Dr. Chang) 做 compliance 跟結構融資，他要聽的是這個層次。

---

## 0. 一個誠實的前提

程式碼是用 AI (Cursor) 照著規格生出來的，不是一行行手敲的。
所以你要能講的，是**設計理由與資料流**，不是語法。這份文件就是幫你講得出來。

---

## 1. 整體：三塊東西，共用一個底層

| 名稱 | 白話 | 對應教授的要求 |
|------|------|----------------|
| `doc_router` | 讀文件（表格、掃描報告、手繪股權圖），轉成 graph 能吃的乾淨格式 | 「做一個 OCR 讀上傳的圖片」 |
| `chart_router` | 使用者問問題，系統自動選對的圖表方式呈現 | 「在對話輸出圓餅圖／時間序列／心智圖」 |
| `kyc_agent` | **重點**。自然語言問題 → Cypher 查詢 → 查 graph → 摘要 + 圖 | roadmap 上的「能不能跟資料庫對話做深入分析」 |

三塊底下共用 `common/`，裡面兩個檔案：
- `config.py` — 設定（用哪個模型、Neo4j 連線資訊）
- `llm.py` — 模型接口。所有要用 AI 的地方都走這裡，換模型只改一處。

---

## 2. 模型跑在哪裡（教授最在意這段）

- **預設 = `mock`（假模型）**：不連任何 AI，給固定答案。目的是任何人 clone 下來、
  不裝任何東西就能跑測試。
- **真的要用模型 = 只有本機 Ollama 一條路**（把 Llama 下載到自己電腦跑）。
- `llm.py` 裡有一個檢查，會**擋掉任何公開網址**，只允許 localhost / 私有網段。
  → 程式碼層面就保證 KYC 資料不可能被送到 ChatGPT 或 Claude。
  這不是一句註解，是真的有一段程式在擋。**這點一定要能講**，因為這就是
  整個「私有模型」承諾的實作。

**重要澄清（教授可能會問）：**
「Private LLM = DeepSeek / Llama」不是指公司是私人的——是指這些模型的
**權重可以下載**，你能放到自己的伺服器跑，資料不出門。Claude / GPT-4o
不公開權重，只能把資料送到對方伺服器。差別在**能不能自己跑**，正確講法是
**open-weight（開放權重）vs closed-weight（封閉權重）**。

---

## 3. 怎麼實際跑起來

進到 `eureka_internship/eureka_internship/` 這層，然後：

```bash
python3 -m venv .venv                        # 建獨立 Python 環境
.venv/bin/pip install -r requirements.txt    # 裝套件
cp .env.example .env                          # 複製設定範本
.venv/bin/python -m pytest -q                 # 跑全部測試
```

> Terminal 若在 Rosetta 模式，指令前加 `arch -arm64`。
> 需要 Python 3.10 以上（`neo4j` 套件要求）。

看 project 9（重點）離線 demo：

```bash
.venv/bin/python -m kyc_agent.demo
```

會印出：**問題 → 產生的 Cypher → 摘要 → 選了哪種圖**。

---

## 4. kyc_agent 內部四關（demo 時就講這個）

一個問題進來，經過四關：

1. **產生器 generator** — 模型看著 graph 結構，把自然語言變成 Cypher。
2. **驗證器 validator** —（安全核心）碰資料庫前先檢查：擋掉所有寫入/刪除、
   限制最多跳幾層、強制加筆數上限。就算模型亂寫也跑不出危險的東西。
3. **執行器 executor** — 真的去 Neo4j 查（本機假資料 或 雲端 Aura 的 80 節點圖）。
4. **解讀器 interpreter** — 把結果變成人話摘要，再交給 `chart_router` 畫圖。

驗證器若擋下來，會把「擋下的理由」丟回模型重寫一次——**只給一次機會**。

四關分開是刻意的：每關能單獨測試、單獨替換。這也是可以跟教授講的設計理由。

---

## 5. 三塊怎麼串起來（一句話）

`doc_router` 把文件變成 graph 資料 → `kyc_agent` 用自然語言查這個 graph
→ `chart_router` 把答案畫出來（時間序列用 Vega-Lite，股權關係用 Cytoscape）。

---

## 6. demo 給教授的 5 分鐘流程

1. 開 terminal 跑 `kyc_agent.demo`，讓每個問題的 Cypher 一個個印出來。
2. 特別點出 UBO（最終受益人）那題 → 交給 `render_graph` 畫關係圖。
3. 點出風險摘要那題 → 交給 `render_chart` 畫圖。
4. （可選）開 `chart_router.demo_server` 看視覺化 UI。
5. 遞上 `kyc_agent/MEMO.md`，問一句：
   *「v2 要做到什麼，您才會願意放到客戶面前？」*

---

## 7. 要誠實說的限制（reklame 反而加分）

- 測試是 **整合/回歸測試**，不代表真實模型的準確率。
- 目前用的是**合成假資料**，不是真的 KYC 資料。
- 驗證器是保守的字串掃描，不是完整 Cypher parser；正式環境還要靠資料庫層級的
  唯讀帳號、網路隔離、query 監控。

> 對做 compliance 的教授，主動講限制比誇大數字更可信。
