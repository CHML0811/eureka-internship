# eval/ — evaluation harness (Task 6).
#
# gold/        hand-labelled YAML ground truth (10 documents, all 5 types)
# run_eval.py  prints per-field precision / recall / F1 broken down by DocumentType
