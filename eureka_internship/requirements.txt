# Core schema and config
pydantic>=2.7
python-dotenv>=1.0

# AWS Textract engine (Task 3)
boto3>=1.34

# Self-hosted Llama (text + vision) via Ollama's OpenAI-compatible API (Task 4).
# The `openai` SDK is used only as an HTTP client against LOCAL_LLM_BASE_URL —
# no calls are made to OpenAI's actual API. See .env.example / EUREKA_MASTER_CONTEXT §4.
openai>=1.30

# Local/private graph database client (Project 9)
neo4j>=6.2

# Evaluation harness (Task 6)
pyyaml>=6.0
scikit-learn>=1.4   # precision_recall_fscore_support

# Testing
pytest>=8.0
pytest-asyncio>=0.23

# NOTE: anthropic SDK intentionally omitted — no code in this project should
# call Anthropic's API with real document/entity data (EUREKA_MASTER_CONTEXT §7).
